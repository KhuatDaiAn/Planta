import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CartHistory = () => {
  return (
    <View style = {{padding: 48}}>
      <Text style = {{fontSize: 40}}>CartHistory</Text>
    </View>
  );
};

export default CartHistory;

const styles = StyleSheet.create({});
